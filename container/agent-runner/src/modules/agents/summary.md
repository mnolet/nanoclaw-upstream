## Persistent companions (`create-agent`)

For sub-agents with their own memory, or parallel non-blocking work that reports back. Address via `send-message --to NAME`. NOT for one-shots — use the SDK `Agent` tool. `module agents` for details.
